import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
import { z } from "zod";
//#region src/routes/auth.tsx
var $$splitComponentImporter = () => import("./auth-0agqo0Ic.js");
var searchSchema = z.object({ mode: z.enum(["signin", "signup"]).optional() });
var Route = createFileRoute("/auth")({
	validateSearch: (s) => searchSchema.parse(s),
	head: () => ({ meta: [{ title: "Sign in — Magnetic Repair" }, {
		name: "description",
		content: "Sign in or create an account with Magnetic Repair."
	}] }),
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
