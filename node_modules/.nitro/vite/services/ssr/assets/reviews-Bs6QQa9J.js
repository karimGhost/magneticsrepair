import { t as supabase } from "./client-aDQb08CI.js";
import { n as useAuth } from "./auth-context-DJtxt_ra.js";
import { t as Button } from "./button-BpE9Czok.js";
import { n as CardContent, t as Card } from "./card-CIcse5Os.js";
import { t as Badge } from "./badge-DHlcf1ty.js";
import { t as Textarea } from "./textarea-Cp94w9lz.js";
import { a as SelectValue, i as SelectTrigger, n as SelectContent, r as SelectItem, t as Select } from "./select-DxHg2FK2.js";
import { useEffect, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { z } from "zod";
import { toast } from "sonner";
import { MessageSquareQuote, Star } from "lucide-react";
//#region src/routes/_authenticated/reviews.tsx?tsr-split=component
var reviewSchema = z.object({
	rating: z.number().int().min(1).max(5),
	comment: z.string().trim().max(1e3).optional()
});
function Stars({ value, onChange }) {
	return /* @__PURE__ */ jsx("div", {
		className: "flex gap-0.5",
		children: [
			1,
			2,
			3,
			4,
			5
		].map((n) => /* @__PURE__ */ jsx("button", {
			type: "button",
			disabled: !onChange,
			onClick: () => onChange?.(n),
			"aria-label": `${n} star${n > 1 ? "s" : ""}`,
			className: onChange ? "transition hover:scale-110" : "cursor-default",
			children: /* @__PURE__ */ jsx(Star, { className: `h-5 w-5 ${n <= value ? "fill-primary text-primary" : "text-muted-foreground"}` })
		}, n))
	});
}
function ReviewsPage() {
	const { user, roles } = useAuth();
	const isAdmin = roles.includes("admin");
	const [reviews, setReviews] = useState([]);
	const [tickets, setTickets] = useState([]);
	const [ticketId, setTicketId] = useState("");
	const [rating, setRating] = useState(5);
	const [comment, setComment] = useState("");
	const [loading, setLoading] = useState(true);
	const load = async () => {
		const [{ data: rv }, { data: tk }] = await Promise.all([supabase.from("reviews").select("*").order("created_at", { ascending: false }), supabase.from("repair_tickets").select("id,tracking_id,brand,model,status").eq("customer_id", user?.id ?? "").in("status", [
			"completed",
			"ready_pickup",
			"collected"
		])]);
		setReviews(rv ?? []);
		setTickets(tk ?? []);
		setLoading(false);
	};
	useEffect(() => {
		if (user) load();
	}, [user]);
	const submit = async () => {
		const parsed = reviewSchema.safeParse({
			rating,
			comment: comment || void 0
		});
		if (!parsed.success) return toast.error(parsed.error.issues[0].message);
		const { error } = await supabase.from("reviews").insert({
			customer_id: user.id,
			ticket_id: ticketId || null,
			rating: parsed.data.rating,
			comment: parsed.data.comment ?? null
		});
		if (error) return toast.error(error.message);
		toast.success("Thanks for your feedback!");
		setComment("");
		setRating(5);
		setTicketId("");
		load();
	};
	const reply = async (id, text) => {
		const { error } = await supabase.from("reviews").update({ reply: text }).eq("id", id);
		if (error) return toast.error(error.message);
		toast.success("Reply posted");
		load();
	};
	const toggleApproved = async (id, approved) => {
		const { error } = await supabase.from("reviews").update({ approved }).eq("id", id);
		if (error) return toast.error(error.message);
		load();
	};
	const avg = reviews.length ? reviews.reduce((s, r) => s + r.rating, 0) / reviews.length : 0;
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-8",
		children: [
			/* @__PURE__ */ jsxs("div", { children: [
				/* @__PURE__ */ jsx("div", {
					className: "text-xs uppercase tracking-widest text-primary font-semibold",
					children: "Feedback"
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "mt-1 text-3xl font-semibold",
					children: "Reviews"
				}),
				reviews.length > 0 && /* @__PURE__ */ jsxs("div", {
					className: "mt-2 flex items-center gap-2 text-sm text-muted-foreground",
					children: [
						/* @__PURE__ */ jsx(Stars, { value: Math.round(avg) }),
						" ",
						avg.toFixed(1),
						" from ",
						reviews.length,
						" review",
						reviews.length > 1 ? "s" : ""
					]
				})
			] }),
			/* @__PURE__ */ jsx(Card, {
				className: "shadow-card",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "p-6 space-y-4",
					children: [
						/* @__PURE__ */ jsx("h2", {
							className: "text-lg font-semibold",
							children: "Leave a review"
						}),
						tickets.length > 0 && /* @__PURE__ */ jsxs(Select, {
							value: ticketId,
							onValueChange: setTicketId,
							children: [/* @__PURE__ */ jsx(SelectTrigger, {
								className: "max-w-sm",
								children: /* @__PURE__ */ jsx(SelectValue, { placeholder: "Which repair? (optional)" })
							}), /* @__PURE__ */ jsx(SelectContent, { children: tickets.map((t) => /* @__PURE__ */ jsxs(SelectItem, {
								value: t.id,
								children: [
									t.tracking_id,
									" · ",
									t.brand,
									" ",
									t.model
								]
							}, t.id)) })]
						}),
						/* @__PURE__ */ jsx(Stars, {
							value: rating,
							onChange: setRating
						}),
						/* @__PURE__ */ jsx(Textarea, {
							value: comment,
							onChange: (e) => setComment(e.target.value),
							maxLength: 1e3,
							placeholder: "Tell us how your repair went…"
						}),
						/* @__PURE__ */ jsx(Button, {
							className: "bg-gradient-primary",
							onClick: submit,
							children: "Publish review"
						})
					]
				})
			}),
			/* @__PURE__ */ jsx(Card, {
				className: "shadow-card",
				children: /* @__PURE__ */ jsxs(CardContent, {
					className: "p-6",
					children: [/* @__PURE__ */ jsx("h2", {
						className: "text-lg font-semibold mb-4",
						children: "What customers say"
					}), loading ? /* @__PURE__ */ jsx("p", {
						className: "text-sm text-muted-foreground",
						children: "Loading…"
					}) : reviews.length === 0 ? /* @__PURE__ */ jsx("div", {
						className: "rounded-xl border border-dashed border-border p-10 text-center text-sm text-muted-foreground",
						children: "No reviews yet."
					}) : /* @__PURE__ */ jsx("div", {
						className: "space-y-5",
						children: reviews.map((r) => /* @__PURE__ */ jsxs("div", {
							className: "rounded-xl border border-border p-4",
							children: [
								/* @__PURE__ */ jsxs("div", {
									className: "flex items-center justify-between gap-3",
									children: [/* @__PURE__ */ jsx(Stars, { value: r.rating }), /* @__PURE__ */ jsxs("div", {
										className: "flex items-center gap-2",
										children: [!r.approved && /* @__PURE__ */ jsx(Badge, {
											variant: "secondary",
											children: "Hidden"
										}), /* @__PURE__ */ jsx("span", {
											className: "text-xs text-muted-foreground",
											children: new Date(r.created_at).toLocaleDateString()
										})]
									})]
								}),
								r.comment && /* @__PURE__ */ jsx("p", {
									className: "mt-2 text-sm",
									children: r.comment
								}),
								r.reply && /* @__PURE__ */ jsxs("div", {
									className: "mt-3 rounded-lg bg-muted p-3 text-sm",
									children: [/* @__PURE__ */ jsxs("div", {
										className: "flex items-center gap-1.5 text-xs font-medium text-primary",
										children: [/* @__PURE__ */ jsx(MessageSquareQuote, { className: "h-3.5 w-3.5" }), " Magnetic Repair"]
									}), /* @__PURE__ */ jsx("p", {
										className: "mt-1",
										children: r.reply
									})]
								}),
								isAdmin && /* @__PURE__ */ jsxs("form", {
									className: "mt-3 flex flex-wrap gap-2",
									onSubmit: (e) => {
										e.preventDefault();
										const input = e.currentTarget.elements.namedItem("reply");
										if (input.value.trim()) reply(r.id, input.value.trim().slice(0, 1e3));
									},
									children: [
										/* @__PURE__ */ jsx("input", {
											name: "reply",
											defaultValue: r.reply ?? "",
											placeholder: "Reply as the shop…",
											maxLength: 1e3,
											className: "flex-1 min-w-48 rounded-md border border-input bg-background px-3 py-2 text-sm"
										}),
										/* @__PURE__ */ jsx(Button, {
											size: "sm",
											type: "submit",
											variant: "outline",
											children: "Reply"
										}),
										/* @__PURE__ */ jsx(Button, {
											size: "sm",
											type: "button",
											variant: "ghost",
											onClick: () => toggleApproved(r.id, !r.approved),
											children: r.approved ? "Hide" : "Publish"
										})
									]
								})
							]
						}, r.id))
					})]
				})
			})
		]
	});
}
//#endregion
export { ReviewsPage as component };
